<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jenking.graduatesinternship">

    <application
        android:name=".application.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 启动页 -->
        <activity android:name=".activitys.common.StartupActivity">

        </activity>

        <!-- 学生主页 -->
        <activity android:name=".activitys.student.StudentMainActivity"></activity>
        <activity android:name=".activitys.common.LoginActivity"></activity>
        <activity android:name=".activitys.common.RegisterActivity"></activity>
        <activity android:name=".activitys.common.SettingActivity"></activity>
        <activity android:name=".activitys.teacher.TeacherMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>